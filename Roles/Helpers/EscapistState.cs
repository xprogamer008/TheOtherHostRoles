namespace TownOfHost.RoleHelpers;

public enum EscapistState
{
    Default = 0,
    OnMark,
    OnRecall
}

public enum TimeTravelerState
{
    Default = 0,
    OnMark,
    OnRecall
}

public enum GodState
{
    Default = 0,
    OnMark,
    OnRecall
}